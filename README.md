markdown
# 🚀 TaskSync: FastAPI + Aiogram + Redis

Комплексное решение для управления задачами, объединяющее мощный бэкенд, интерактивный веб-интерфейс и умного Telegram-бота.

## ✨ Ключевые возможности
- **Cross-platform Sync:** Общая база данных для Web и Telegram.
- **Smart Web UI:** Редактирование задач "на лету" (Inline Editing) и асинхронный CRUD.
- **Secure Auth:** Авторизация по стандарту OAuth2/JWT.
- **Bot FSM:** Управление состояниями регистрации и фильтрации задач через Redis.
- **Admin Dashboard:** Интерфейс для мониторинга зарегистрированных пользователей.

## 🏗 Архитектура
- **API:** FastAPI (Asynchronous)
- **Бот:** Aiogram 3.x
- **БД:** SQLAlchemy 2.0 + Alembic
- **Кэш/Состояния:** Redis
- **Frontend:** Jinja2 + Vanilla JS (ES2026)

## 🔧 Запуск проекта
1. Настройте `.env` (TOKEN, SECRET_KEY, DATABASE_URL).
2. Поднимите Redis: `docker run -d -p 6379:6379 redis`.
3. Запустите бэкенд: `uvicorn backend.main:app --reload`.
4. Запустите бота: `python -m bot.bot`.
